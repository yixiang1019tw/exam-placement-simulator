# 落點雷達：年度 JSON 版

本資料夾採用「**HTML 顯示介面、JSON 保存學校資料**」的分工。`index.html` 不再內嵌學校清單；使用者選擇會考年度後，頁面會依相對路徑讀取同一資料夾中的 `schools_年度.json`，例如 2026 年會讀取 `./schools_2026.json`。

| 檔案 | 用途 |
|---|---|
| `index.html` | 落點雷達介面與計算邏輯，不保存學校清單。 |
| `schools_2026.json` | 2026 會考／115 學年度的逐校資料，目前含 309 所學校記錄。 |
| `official_rules_115.json` | 115 學年度就學區規則與官方來源索引。 |
| `AI_逐校官方資料轉JSON指令.md` | 交給 AI 執行官方資料蒐集、核對與 JSON 生成的完整指令。 |
| `index.js` | 由 HTML script 區塊抽出的語法檢查副本，可刪除，不是頁面必要檔案。 |

若要新增年度，只需把合法 JSON 放在同一資料夾，檔名使用 `schools_2025.json`、`schools_2024.json` 等格式。HTML 會依下拉選單的年度值自動組合檔名；若該年度尚未有檔案，畫面只會顯示「本年度資料整理中，請先選擇已有資料的年度」，不會呈現技術錯誤。

直接雙擊 `index.html` 可以觀看介面，但部分瀏覽器會限制 `file://` 頁面讀取同目錄 JSON。若要使用年度 JSON 自動載入，建議從本資料夾啟動任一個靜態伺服器，再以瀏覽器開啟該 HTTP 網址。正式部署到一般網站時，HTML 與所有 `schools_YYYY.json` 放在同一個網址目錄即可。

生肖只作為已達最低標準後的非直接、低權重可能性微調；它不會寫入正式採計科目、最低門檻、招生名額或官方比序，也不能讓未達最低標準的學校變成有機會。
